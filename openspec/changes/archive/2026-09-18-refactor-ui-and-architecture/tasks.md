## 1. Phase 0: Event infrastructure and critical fixes (no file moves)

- [x] 1.1 Create `js/engine/events.js` exposing `on(type, handler)` / `emit(type, payload)` with per-handler exception isolation and the event-type constants (`init`, `stage_start`, `stage_success`, `log`, `error`, `safe_to_exit`, `reboot_required`, plus `telemetry`, `ui-status`, `progress`). Verify: file contains no DOM references (`git grep -n "document\.\|getElementById" -- js/engine/events.js` returns nothing) and `node --check js/engine/events.js` passes.
- [x] 1.2 Create `js/engine/telemetry.js` as a subscriber that performs the existing beacon XHR to the relative path `t`, preserving each event's `tag` and `detail` exactly as the chains posted them before. Verify: `git grep -n "XMLHttpRequest" -- js/*.js` finds it only in this module, no DOM references, and `node --check js/engine/telemetry.js` passes.
- [x] 1.3 Rewire `chain_lapse.js` wrappers `mark()` / `check()` / `state()` / `post()` to emit through `js/engine/events.js`, preserving its own `terse()`/`PROSE` compaction and its beacon detail behavior, and registering the transitional in-page subscriber for `#out` / `#state`. Verify: after the change, a hardware run shows the same `PROOF-*` tags in the same order as before, and the only DOM access left in the file is the transitional subscriber.
- [x] 1.4 Rewire `chain_poops.js` the same way, preserving its `mark`/`trace` split (`PRIMITIVE_LOUD` filter) and raw-detail beacon. Verify: `PRIMITIVE_LOUD` still routes non-loud engine events to beacon-only, a hardware run shows the same tag set as before, and `node --check chain_poops.js` passes.
- [x] 1.5 Add `stage_start` emissions at the entry of stages 1-10 and `stage_success` at each existing stage-completion point in both chains; a failed stage proof must not emit `stage_success` for that stage. Verify: hardware logs show the full `stage_start`/`stage_success` sequence and no success after a `PROOF-FAIL` for the same stage.
- [x] 1.6 Fix `chain_lapse.js:1` to import `./core.js?v=10`, pass an `onEvent` callback to `installWindowP`, and log a `PAIR-STATUS` line after installation (parity with `chain_poops.js:215-219`). Verify: `git grep -n 'from "./core.js' -- chain_lapse.js` shows the `?v=10` specifier, and a hardware run on 11.00 (or 12.02) logs `PAIR-STATUS` plus a `PAIR-UP released=...` or an explicit fallback line.
- [x] 1.7 Align the README "Tested on hardware" table with `fw_status` in `ps4_offsets.js`: 11.00/11.50/13.00 proven; 12.00 verified-vs-dump but untested; 12.02 shares 12.00; 12.50 asserted table, unverified; 12.52 shares 12.50. Verify: each README row's wording matches the state recorded in the firmware's `fw_status` string.
- [x] 1.8 Create `tools/appcache-manifest.mjs` (Node ESM, zero dependencies): write mode recomputes lowercase SHA-256 digests in place preserving entry order; `--check` writes nothing and exits non-zero on drift, missing file, or unlisted runtime file; `?query` is stripped for filesystem lookup so both core entries hash the same file; dev files (`AGENTS.md`, `openspec/**`, `tools/**`, `.github/**`, `README.md`, `LICENSE`) are never entries. Verify: `node tools/appcache-manifest.mjs --check` exits 0 on the unchanged tree; after touching one runtime file it exits non-zero naming the entry; the manifest file is unchanged after `--check`.
- [x] 1.9 Update `cache.appcache` for all Phase 0 runtime changes (add `js/engine/events.js`, `js/engine/telemetry.js`; refresh chain digests; keep both `core.js` entries identical) and run the check mode. Verify: `--check` exits 0, and a console first-run caches followed by an offline second load reaches the same PROOF tags.
- [ ] 1.10 Phase 0 hardware gate: run on 11.00 and 13.00 and confirm tag/order parity with the pre-change run, exactly one terminal event (`safe_to_exit` or `reboot_required`), unchanged beacon traffic, and visible lapse promotion telemetry. Verify: recorded logs show no missing or renamed tag and a terminal line matching the teardown outcome.

- [x] 1.11 Emit lifecycle events (`init` after firmware resolution and exactly one of `safe_to_exit` / `reboot_required` at teardown) in both chains, gated by the engine's own outcome flags. Verify: `git grep -n "EV.INIT\|EV.SAFE_TO_EXIT\|EV.REBOOT_REQUIRED" -- chain_lapse.js chain_poops.js` shows all three in both files.

## 2. Phase 1: UI extraction and CSS modernization

- [x] 2.1 Create `js/ui/logview.js`: append-only rendering with a batched flush (~100 ms), immediate flush on `error`/`safe_to_exit`/`reboot_required` and `pagehide`, FIFO line cap, and auto-scroll only when already at the bottom. Verify: no code path rebuilds the whole log via `innerHTML`; a local smoke page renders 300+ events with one node per line and no visible lag.
- [x] 2.2 Create `js/ui/status.js` subscribing to `init`, `ui-status`, `stage_start` and `stage_success` to update the status line. Verify: a smoke run driven by the event stream shows stage text and classes changing.
- [x] 2.3 Create `js/ui/toast.js` for `error`, `safe_to_exit` and `reboot_required` notifications using DOM/CSS only. Verify: each of the three events produces a visible toast with no layout shift and no external asset request.
- [x] 2.4 Create `js/ui/detect.js` and delegate `index.html` firmware detection, AppCache bootstrap and routing to it, preserving the `?bug=lapse|poops` override and every cache status label (`checking`, `cached`, `updateready`, `error`, `obsolete`). Verify: a local smoke test shows the unsupported-UA message, forced-bug routing and each cache state label unchanged.
- [x] 2.5 Create `assets/css/base.css` and `assets/css/theme.css`; remove the inline `<style>` blocks from the three HTML entry points; replace `height:calc(100vh - 141px)` with a flex column; use system fonts only. Verify: `git grep -n "https\?://\|@font-face" -- assets/css` returns nothing and the smoke page fills the viewport with the new theme applied.
- [x] 2.6 Register the UI subscribers at shell start and delete the transitional in-chain subscriber; `chain_lapse.js` and `chain_poops.js` must contain no DOM access. Verify: `git grep -n "document\.\|getElementById\|innerHTML" -- chain_lapse.js chain_poops.js` returns nothing and a chain run still produces the same log lines through `js/ui/logview.js`.
- [x] 2.7 Update `cache.appcache` with the new CSS/UI files and refreshed HTML/chain digests, then run the check mode. Verify: `--check` exits 0 and a console offline load renders the new UI.
- [ ] 2.8 Phase 1 hardware gate on 11.00 and 13.00: first run and offline second run, confirming stages/proofs/toasts render, the terminal event matches the teardown, and PROOF counts show no regression. Verify: recorded logs and screenshots from both firmwares.

## 3. Phase 2: Modular reorganization and manifest rewrite

- [x] 3.1 Record the SHA-256 of every engine file, then relocate `core.js`, `mem.js`, `int64.js`, `ps4_offsets.js`, `chain_lapse.js`, `chain_poops.js`, `rpc_worker.js`, `js/engine/events.js` and `js/engine/telemetry.js` into `js/engine/` without content changes. Verify: each file's post-move digest equals its pre-move digest.
- [x] 3.2 Move `logo_raw.png` to `assets/img/logo_raw.png` and update its three `<img src>` references. Verify: the digest is unchanged and `git grep -n 'src="logo_raw.png"' -- *.html` returns nothing.
- [x] 3.3 Update the shell imports to `./js/engine/chain_lapse.js` and `./js/engine/chain_poops.js`. Verify: `git grep -n "chain_lapse\|chain_poops" -- *.html` shows only the new paths and a smoke page loads both modules.
- [x] 3.4 Update the two `new Worker(...)` sites to `js/engine/rpc_worker.js` (document-relative). Verify: a smoke run creates the worker without a `WORKER-ONERROR` line.
- [x] 3.5 Rewrite every `cache.appcache` entry and `FALLBACK` line for the new paths in one revision: exact URLs for all module imports, the worker, stylesheets, images, `payload.bin` and `patches/*.bin`, including both core entries. Verify: `node tools/appcache-manifest.mjs --check` exits 0 and a grep audit of `import`/`fetch`/`new Worker`/`link`/`img` URLs finds a matching manifest entry for each.
- [x] 3.6 Update path references in `AGENTS.md` to the new locations (dev file; never a manifest entry). Verify: `--check` still exits 0 and no stale root-level engine path remains in `AGENTS.md`.
- [ ] 3.7 Phase 2 hardware gate on 11.00 and 13.00: first run after the move plus a fully offline run, confirming identical PROOF results and recording the before/after digest table for all moved files. Verify: recorded logs and digest comparison show no content change and a fully working offline load.
